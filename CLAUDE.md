# WMH-STARTER

공유숙박 호스트가 **자기 숙소의 게스트 안내 앱**을 만들어 보는 수업용 껍데기다.
운영 중인 앱이 아니라 수강생이 Fork 해서 채워 넣는 빈 틀이다.

빌드 도구도 `package.json` 도 없다. **도입하지 마라.** 파일을 그대로 올리면 그게 배포다.

## 구조

```
index.html    머리말 · 게이트 · 하단 탭 · 화면 불러오는 부분   (6KB 안쪽)
style.css     겉모습. 2주차에 :root 의 색만 만진다
content.js    숙소 정보. 수강생이 4주 내내 만지는 유일한 파일
app.js        앱을 움직이는 부분. 거의 손대지 않는다
screens/*.html  화면 하나에 파일 하나 (v-checkin → checkin.html)
img/          자리만 잡아 둔 회색 그림들
```

## 고칠 때 가장 먼저 볼 곳

**`content.js`.** 숙소 이름·주소·연락처·와이파이·좌표·가게 목록·화면 목록이 전부 여기 있다.
운영 정보 변경은 대부분 이 파일 하나만 고치면 끝난다.
content.js 로 해결되는 일을 screens 의 HTML 을 뒤져서 고치지 마라.

## 규칙

- **화면 켜고 끄기**는 `content.js` 의 `screens` 목록 한 줄로 한다.
  목록에서 빠진 화면은 파일이 있어도 실리지 않고, 그 화면으로 가는 버튼은 app.js 가 숨긴다.
- **화면은 fetch 로 불러온다.** 그래서 `file://` 로 열면 안 되고 로컬 서버가 필요하다.
  한 화면을 못 불러와도 나머지는 살아 있어야 한다. 이 성질을 깨지 마라.
- **좌표가 없으면 지도는 나타나지 않는다.** `content.js` 에 lat·lng 을 채우면 저절로 켜진다.
- **글은 한국어만 있다.** app.js 의 언어 바꾸기 기능은 살아 있지만
  `content.js` 의 `languages` 가 하나뿐이라 버튼이 숨어 있다. `'en'` 을 더하면 다시 나온다.
- 실제 숙소 정보(주소·전화·좌표·상호)를 코드에 박아 넣지 마라. 전부 `content.js` 를 거친다.

## 확인하는 법

```
npx http-server -p 8000     # 로컬 서버를 띄우고 http://127.0.0.1:8000 을 연다
```

화면을 켜고 끄거나 좌표를 넣은 뒤에는 브라우저 콘솔에 빨간 줄이 없는지 본다.
